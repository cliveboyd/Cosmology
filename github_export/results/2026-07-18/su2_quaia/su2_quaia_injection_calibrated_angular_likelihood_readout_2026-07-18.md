# SU2 / Quaia Injection-Calibrated Angular-Likelihood Readout

Date: 18 July 2026

Outcome: **calibration failure and underpowered**.

Validation false-positive rate was `0.014` and joint 1x recovery was `0`. Observed pooled held-out gain was `-11653.4` against a frozen threshold of `413.749`, with `0` positive sectors, amplitude ratio `0.92707` and direction separation `113.615 deg`.

The untouched null false-positive rate exceeded 1%, and the locked 1x effect was not jointly recovered in at least 80% of validation injections. Both are calibration outcomes: the threshold remains unchanged and the observed candidate is not promotable.

In-sample BIC is non-gating and cannot replace held-out gain.
