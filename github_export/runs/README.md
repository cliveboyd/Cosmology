# Runs

This folder stores dated launcher scripts for reproducible long-running diagnostics.

These are useful for recording how a run was started, including the exact script,
output directory, random seed, and mock count. Runtime logs and partial outputs
are intentionally excluded from Git.

